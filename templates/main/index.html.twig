{% extends 'base.html.twig' %}

{% block title %}Hello MainController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		{% if app.user %}
			{% if app.user.username != "sapa" %}
				<h1>Bienvenido usuario:
					{{ app.user.username }}</h1>
				<ul>
					<li>
						<a href="/sorteo/show">Ver Sorteos</a><br>
					</li>
					<li>
						<a href="{{ path('app_user_addMoney') }}">Añadir saldo</a><br>
					</li>
					<li>
						<a href="/compra/participe">Ver Sorteos Participados</a>
					</li>
				</ul>
			{% else %}
				<a href="/sorteo/new">Crear Sorteos</a>
			{% endif %}
		{% endif %}
	</div>
{% endblock %}
