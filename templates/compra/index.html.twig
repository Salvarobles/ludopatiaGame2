{% extends 'base.html.twig' %}

{% block title %}Compra index{% endblock %}

{% block body %}
    <h1>Compra index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NumeroLoteria</th>
                <th>FechaCompra</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compra in compras %}
            <tr>
                <td>{{ compra.id }}</td>
                <td>{{ compra.numeroLoteria }}</td>
                <td>{{ compra.fechaCompra ? compra.fechaCompra|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_compra_show', {'id': compra.id}) }}">show</a>
                    <a href="{{ path('app_compra_edit', {'id': compra.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_compra_new') }}">Create new</a>
{% endblock %}
